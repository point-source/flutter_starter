/// Smoke test for the Flutter starter template.
///
/// Verifies that the app bootstraps without errors. Full tests for each
/// feature are located in their respective directories under `test/`.
library;

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder test', () {
    // Placeholder until feature-specific tests are added.
    expect(1 + 1, equals(2));
  });
}
